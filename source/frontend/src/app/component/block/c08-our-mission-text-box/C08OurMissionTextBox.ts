import AbstractBlock from '../AbstractBlock';

export default class C08OurMissionTextBox extends AbstractBlock {
  public static readonly displayName: string = 'c08-our-mission-text-box';

  constructor(el: HTMLElement) {
    super(el);
  }

  public dispose() {
    super.dispose();
  }
}
