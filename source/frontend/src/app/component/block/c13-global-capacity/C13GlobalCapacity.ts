import AbstractBlock from '../AbstractBlock';

export default class C13GlobalCapacity extends AbstractBlock {
  public static readonly displayName: string = 'c13-global-capacity';

  constructor(el: HTMLElement) {
    super(el);
  }

  public dispose() {
    super.dispose();
  }
}
