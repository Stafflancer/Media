import AbstractBlock from '../AbstractBlock';

export default class C21VideoPlayerSection extends AbstractBlock {
  public static readonly displayName: string = 'c21-video-player-section';

  constructor(el: HTMLElement) {
    super(el);
  }

  public dispose() {
    super.dispose();
  }
}
