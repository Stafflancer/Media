<link rel="stylesheet" href="./cta-chevron.scss">

<a href="{{#if email}}mailto:{{/if}}{{link}}" class="cta cta-chevron{{#if class}} {{class}}{{/if}}"{{#if data-attr}} data-{{data-attr}}{{/if}}>
  <span class="copy">{{copy}}</span>
  {{> general/icon name="chevron"}}
</a>
