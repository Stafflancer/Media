<picture{{#if class}} class="{{class}}"{{/if}}{{#if data-attr}} data-{{data-attr}}{{/if}}>
  {{#each source}}
  <source srcset="{{srcset}}" media="{{media}}">
  {{/each}}
  <img src="{{src}}" class="visual" alt="{{alt}}" />
</picture>
